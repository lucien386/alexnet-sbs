
import os
class batch_helper(object):
    def __init__(self, path):
        self.path = path
        self.batch = []

    def load(self):
        for filename in os.listdir(path):
            barch.append()

#Graphing

# The creation of graph
